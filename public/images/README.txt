Drop your real images in here using these exact filenames and they'll
automatically replace the dashed placeholder boxes on the site — no code
changes needed.

Currently used:
- founder.jpg   → Founder's Message section on the Home page

You can add more images later (e.g. team.jpg, campus.jpg) as new sections
get built — just reference the path "/images/<filename>" via the
<ImageSlot /> component in src/components/ImageSlot.jsx.
